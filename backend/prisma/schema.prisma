generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model WorkerScheduler {
  id        Int      @id @default(autoincrement())
  nextFetch DateTime
}
